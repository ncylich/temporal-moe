diff --git a/megatron/core/transformer/moe/experts.py b/megatron/core/transformer/moe/experts.py
index fc0cf3e6..1b181d46 100644
--- a/megatron/core/transformer/moe/experts.py
+++ b/megatron/core/transformer/moe/experts.py
@@ -1,3 +1,4 @@
+import os
 # Copyright (c) 2024, NVIDIA CORPORATION. All rights reserved.
 
 import copy
@@ -88,6 +89,71 @@ def expert_dist_ckpt_decorator(func):
     return wrapper
 
 
+class _PadRows(torch.autograd.Function):
+    """out[p] = x[src[p]] * valid[p]; padded rows are exactly zero. Backward is a plain gather."""
+
+    @staticmethod
+    def forward(ctx, x, src, valid, pos):
+        ctx.save_for_backward(pos)
+        return x.index_select(0, src).mul_(valid)
+
+    @staticmethod
+    def backward(ctx, grad):
+        (pos,) = ctx.saved_tensors
+        return grad.index_select(0, pos), None, None, None
+
+
+class _UnpadRows(torch.autograd.Function):
+    """out[i] = y[pos[i]]. Backward: grad_y[p] = grad[src[p]] with NO mask: padded input rows
+    are exactly zero, so fc1 = 0 there, the GLU and its derivative are 0 there, and every padded
+    contribution to dw1, dw2 and dx vanishes regardless of grad_y on padded rows."""
+
+    @staticmethod
+    def forward(ctx, y, pos, src):
+        ctx.save_for_backward(src)
+        return y.index_select(0, pos)
+
+    @staticmethod
+    def backward(ctx, grad):
+        (src,) = ctx.saved_tensors
+        return grad.index_select(0, src), None, None
+
+
+def _torch_grouped_mm_experts(x, w1, w2, tokens_per_expert, activation_func, align=16):
+    """Sync-free replacement for the two legacy grouped_gemm calls (MOE_TORCH_GMM=1).
+
+    grouped_gemm's CUTLASS wrapper does host-side work on every call, which serialises the CPU
+    against the GPU (measured 2026-09-02: 124 s of a 136 s iteration on the host, GPU 15% busy).
+    torch._grouped_mm takes device-side offsets, so nothing here touches the host. Each expert's
+    rows are padded (device-side, into a fixed n + align*E buffer) to a multiple of `align` with
+    at least `align` rows, because the torch 2.8 _grouped_mm backward hangs on an empty group.
+    Padded rows are zero and dropped afterwards, so real rows and all gradients are bit-identical
+    to the grouped GEMM (checked fwd + dx/dw1/dw2, including empty experts).
+    """
+    if x.shape[-1] % 8 or w1.shape[-1] % 8 or w2.shape[-1] % 8:
+        return None  # _grouped_mm needs 16-byte (8 x bf16) row strides; caller uses grouped_gemm
+    n = x.shape[0]
+    E = w1.shape[0]
+    counts = tokens_per_expert.to(device=x.device, dtype=torch.int64, non_blocking=True)
+    pc = ((counts + align - 1) // align * align).clamp_(min=align)
+    poffs = torch.cumsum(pc, 0)
+    coffs = torch.cumsum(counts, 0)
+    rows = torch.arange(n, device=x.device)
+    g = torch.searchsorted(coffs, rows, right=True).clamp_(max=E - 1)
+    pos = (poffs - pc)[g] + rows - (coffs - counts)[g]  # padded position of each real row
+    ppos = torch.arange(n + align * E, device=x.device)
+    pg = torch.searchsorted(poffs, ppos, right=True).clamp_(max=E - 1)
+    within = ppos - (poffs - pc)[pg]
+    valid = (within < counts[pg]).to(x.dtype).unsqueeze_(1)
+    src = ((coffs - counts)[pg] + within).clamp_(max=n - 1)  # source row of each padded row
+    xp = _PadRows.apply(x, src, valid, pos)
+    offs = poffs.to(torch.int32)
+    h = torch._grouped_mm(xp, w1, offs=offs)
+    h = activation_func(h)
+    y = torch._grouped_mm(h, w2, offs=offs)
+    return _UnpadRows.apply(y, pos, src)
+
+
 class GroupedMLP(MegatronModule):
     """An efficient implementation of the Experts layer using GroupedGEMM.
 
@@ -218,13 +284,23 @@ class GroupedMLP(MegatronModule):
             w1 = self.weight1.view(self.num_local_experts, self.config.hidden_size, -1)
             w2 = self.weight2.view(self.num_local_experts, -1, self.config.hidden_size)
 
-            fc1_output = gg.ops.gmm(
-                permuted_local_hidden_states, w1, tokens_per_expert, trans_b=False
-            )
+            fc2_output = None
+            if os.environ.get("MOE_TORCH_GMM") == "1":
+                fc2_output = _torch_grouped_mm_experts(
+                    permuted_local_hidden_states, w1, w2, tokens_per_expert, self.activation_func
+                )
+            if fc2_output is None:
+                if tokens_per_expert.is_cuda:  # sync-free dispatcher left the counts on device
+                    tokens_per_expert = tokens_per_expert.cpu()
+                fc1_output = gg.ops.gmm(
+                    permuted_local_hidden_states, w1, tokens_per_expert, trans_b=False
+                )
 
-            intermediate_parallel = self.activation_func(fc1_output)
+                intermediate_parallel = self.activation_func(fc1_output)
 
-            fc2_output = gg.ops.gmm(intermediate_parallel, w2, tokens_per_expert, trans_b=False)
+                fc2_output = gg.ops.gmm(
+                    intermediate_parallel, w2, tokens_per_expert, trans_b=False
+                )
         else:
             # No token is allocated for local experts.
             assert torch.count_nonzero(tokens_per_expert) == 0
diff --git a/megatron/core/transformer/moe/token_dispatcher.py b/megatron/core/transformer/moe/token_dispatcher.py
index 54a89eec..6dc83dcc 100644
--- a/megatron/core/transformer/moe/token_dispatcher.py
+++ b/megatron/core/transformer/moe/token_dispatcher.py
@@ -1,3 +1,4 @@
+import os
 # Copyright (c) 2024, NVIDIA CORPORATION. All rights reserved.
 
 from abc import ABC, abstractmethod
@@ -396,7 +397,16 @@ class MoEAlltoAllTokenDispatcher(MoETokenDispatcher):
         else:
             # Dropless
             self.num_out_tokens = routing_map.size(0) * self.config.moe_router_topk
-            if self.ep_size > 1 or self.num_local_experts > 1:
+            if (
+                os.environ.get("MOE_TORCH_GMM") == "1"
+                and self.ep_size == 1
+                and self.tp_size == 1
+                and self.config.moe_permute_fusion
+            ):
+                # Sync-free expert path (see experts._torch_grouped_mm_experts): counts stay on
+                # the device, nothing downstream needs a host value.
+                self.cuda_sync_point = "no_sync"
+            elif self.ep_size > 1 or self.num_local_experts > 1:
                 # Token dropless and enable ep. A synchronization is needed before expert parallel
                 # AlltoAll communication to get the `input_splits` and `output_splits` CPU values.
                 self.cuda_sync_point = "before_ep_alltoall"
@@ -458,9 +468,12 @@ class MoEAlltoAllTokenDispatcher(MoETokenDispatcher):
             num_global_tokens_per_local_expert = num_local_tokens_per_expert.reshape(
                 self.num_experts
             )
-            num_tokens_per_local_expert = num_local_tokens_per_expert.to(
-                torch.device("cpu"), non_blocking=True
-            )
+            if self.cuda_sync_point == "no_sync":
+                num_tokens_per_local_expert = num_local_tokens_per_expert
+            else:
+                num_tokens_per_local_expert = num_local_tokens_per_expert.to(
+                    torch.device("cpu"), non_blocking=True
+                )
 
         if self.num_local_experts > 1:
             # [tp_size * ep_size, num_local_experts]. Represents the number of tokens sent
@@ -541,7 +554,12 @@ class MoEAlltoAllTokenDispatcher(MoETokenDispatcher):
             )
 
         # Permutation 2: Sort tokens by local expert.
-        if self.num_local_experts > 1:
+        # With a single EP x TP rank the chunk sort is the identity permutation (chunks are
+        # already in local-expert order), so the sync-free path skips it in both directions.
+        skip_chunk_sort = (
+            os.environ.get("MOE_TORCH_GMM") == "1" and self.tp_size * self.ep_size == 1
+        )
+        if self.num_local_experts > 1 and not skip_chunk_sort:
             if self.drop_and_pad:
                 global_input_tokens = (
                     global_input_tokens.view(
@@ -589,8 +607,11 @@ class MoEAlltoAllTokenDispatcher(MoETokenDispatcher):
         """
         assert bias is None, "Bias is not supported in MoEAlltoAllTokenDispatcher"
 
-        # Unpermutation 2: Unsort tokens by local expert.
-        if self.num_local_experts > 1:
+        # Unpermutation 2: Unsort tokens by local expert (identity on a single EP x TP rank).
+        skip_chunk_sort = (
+            os.environ.get("MOE_TORCH_GMM") == "1" and self.tp_size * self.ep_size == 1
+        )
+        if self.num_local_experts > 1 and not skip_chunk_sort:
             if self.drop_and_pad:
                 hidden_states = (
                     hidden_states.view(
